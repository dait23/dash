import React from "react";
import { observer } from "mobx-react";


export default observer(({ mdmStore }) =>
  <div>
    <ul>
            

          </ul>
    <button>increment</button>
  </div>
);

